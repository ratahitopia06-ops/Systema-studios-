{
  "name": "gorse-force",
  "version": "1.1.0",
  "description": "Static business site and the Recursive Observer reasoning-agent surface.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "nodemailer": "^9.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
